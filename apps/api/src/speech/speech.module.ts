import { Module } from '@nestjs/common';

/**
 * Stub for Phase 0. `POST /speech/synthesize` and the Azure Speech
 * integration land in Phase A (A6) — see PROJECT_PLAN.md §5.1.
 */
@Module({})
export class SpeechModule {}
